---
title: "Spinner"
---
<Column gap="12">
    <Column gap="8">
        <Text as="h1" size="5xl">Spinner</Text>
    </Column>
    <Column gap="8">
        <Text>A simple component that requires only the size from you. </Text>
        <Text as="h2" size="2xl">Demos</Text>
    </Column>
    <Text>A Small example with several sizes.</Text>
    <Row expand items="center" justify="between">
        {[20, 25, 30, 40, 50, 60].map((size) => ( <Spinner size={size} /> ))}
    </Row>
    <CodeBlock code={`

import { Spinner } from '@flowui/react/feedback';

export default Spinners = () => {
    return (
        <Row expand items="center" justify="between">
            <Spinner size={20} />
            <Spinner size={25} />
            <Spinner size={30} />
            <Spinner size={40} />
            <Spinner size={50} />
            <Spinner size={60} />
        </Row>
    );
};
    `} />

    <Column gap="8">
        <Text as="h2" size="2xl">
            API
        </Text>

        <Column gap="4">
            <Text as="h3" size="xl">
                Import
            </Text>
            <CodeBlock code={`
import { Spinner } from "@flowui/react";
// or
import { Spinner } from "@flowui/react/feedback";
// or
import Spinner from "@flowui/react/feedback/spinner";
`}/>
        </Column>

        <Column gap="4">
            <Text as="h3" size="xl">
                Props
            </Text>
            <Table
                columns={[
                    {label: "Name", dataKey: "name"},
                    {label: "Type", dataKey: "type"},
                    {label: "Required", dataKey: "required"},
                    {
                        label: "Default",
                        dataKey: "default",
                        format: (value) => JSON.stringify(value),
                    },
                    {label: "Description", dataKey: "description"},
                ]}
                data={[
                    {
                        name: "size",
                        type: "number",
                        default: 30,
                        required: false,
                        description: "Spinner size",
                    },
                    ]}
                shadow={4}
            />
        </Column>
    </Column>
</Column>